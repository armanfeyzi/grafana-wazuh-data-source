apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  - configmap-datasource.yaml

# Uncomment after creating secret-datasource.yaml from the example:
#  - secret-datasource.yaml

title: Wazuh Grafana Datasource
description: Open-source Grafana plugin for Wazuh — alerts, vulnerabilities, FIM, SCA, and agent status.
theme: jekyll-theme-cayman
markdown: GFM

# Publish docs/*.md at /installation/ etc. (pretty permalinks)
permalink: pretty

plugins:
  - jekyll-relative-links

relative_links:
  enabled: true
  collections: true
